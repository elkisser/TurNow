{% extends 'base.html.twig' %}

{% block title %}Nuevo Usuario - Admin TurNow{% endblock %}

{% block css %}
    {{ parent() }}
    <style>
        .admin-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 0;
        }
        
        .admin-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 2.5rem;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
        }
        
        .duration-badge {
            display: inline-block;
            padding: 8px 16px;
            margin: 4px;
            background: #f8f9fa;
            border: 2px solid #dee2e6;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 14px;
            font-weight: 500;
        }
        
        .duration-badge:hover, .duration-badge.selected {
            background: #667eea;
            color: white;
            border-color: #667eea;
            transform: translateY(-2px);
        }
    </style>
{% endblock %}

{% block content %}
<section class="admin-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="admin-card">
                    <div class="text-center mb-4">
                        <i class="fas fa-user-plus fa-3x text-primary mb-3"></i>
                        <h2 class="h3 text-dark">Crear Nuevo Usuario</h2>
                        <p class="text-muted">Agregar un nuevo usuario al sistema</p>
                    </div>

                    <form method="post" id="nuevoUsuarioForm">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="nombre" class="form-label">Nombre Completo</label>
                                    <input type="text" 
                                           class="form-control form-control-lg" 
                                           id="nombre" 
                                           name="nombre" 
                                           autocomplete="name"
                                           required>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="email" class="form-label">Email</label>
                                    <input type="email" 
                                           class="form-control form-control-lg" 
                                           id="email" 
                                           name="email" 
                                           autocomplete="email"
                                           required>
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="password" class="form-label">Contraseña</label>
                            <input type="password" 
                                   class="form-control form-control-lg" 
                                   id="password" 
                                   name="password" 
                                   autocomplete="new-password"
                                   placeholder="Dejar vacío para contraseña temporal">
                            <div class="form-text">Si se deja vacío, se generará una contraseña temporal.</div>
                        </div>

                        <div class="mb-3">
                            <label for="plan" class="form-label">Plan</label>
                            <select class="form-select form-select-lg" 
                                    id="plan" 
                                    name="plan" 
                                    autocomplete="off"
                                    required>
                                <option value="">Seleccionar plan</option>
                                <option value="basico">Básico</option>
                                <option value="profesional">Profesional</option>
                                <option value="empresa">Empresa</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <fieldset>
                                <legend class="form-label">Duración de Suscripción</legend>
                                <div role="group" aria-labelledby="duration-legend">
                                    <span class="duration-badge" 
                                          data-duration="0" 
                                          role="button" 
                                          tabindex="0" 
                                          aria-pressed="false">Permanente</span>
                                    <span class="duration-badge" 
                                          data-duration="3" 
                                          role="button" 
                                          tabindex="0" 
                                          aria-pressed="false">3 meses</span>
                                    <span class="duration-badge" 
                                          data-duration="6" 
                                          role="button" 
                                          tabindex="0" 
                                          aria-pressed="false">6 meses</span>
                                    <span class="duration-badge" 
                                          data-duration="12" 
                                          role="button" 
                                          tabindex="0" 
                                          aria-pressed="false">1 año</span>
                                    <span class="duration-badge" 
                                          data-duration="24" 
                                          role="button" 
                                          tabindex="0" 
                                          aria-pressed="false">2 años</span>
                                </div>
                                <input type="hidden" 
                                       id="duracion" 
                                       name="duracion" 
                                       value="0"
                                       autocomplete="off">
                                <div class="form-text">Selecciona la duración de la suscripción. "Permanente" = sin fecha de vencimiento.</div>
                            </fieldset>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-premium btn-lg">
                                <i class="fas fa-user-plus me-2"></i>Crear Usuario
                            </button>
                            <a href="{{ path('admin_dashboard') }}" class="btn btn-outline-secondary btn-lg">
                                <i class="fas fa-arrow-left me-2"></i>Volver al Dashboard
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
    console.log('Nuevo usuario - DOM loaded');
    
    // Duration selection
    document.querySelectorAll('.duration-badge').forEach(badge => {
        badge.addEventListener('click', function() {
            selectDurationBadge(this);
        });
        
        // Add keyboard support
        badge.addEventListener('keydown', function(e) {
            if (e.key === 'Enter' || e.key === ' ') {
                e.preventDefault();
                selectDurationBadge(this);
            }
        });
    });
    
    function selectDurationBadge(badge) {
        console.log('Badge clicked:', badge.getAttribute('data-duration'));
        
        // Remove selected class and aria-pressed from all badges
        document.querySelectorAll('.duration-badge').forEach(b => {
            b.classList.remove('selected');
            b.setAttribute('aria-pressed', 'false');
        });
        
        // Add selected class and aria-pressed to clicked badge
        badge.classList.add('selected');
        badge.setAttribute('aria-pressed', 'true');
        
        // Update hidden input value
        const duration = badge.getAttribute('data-duration');
        document.getElementById('duracion').value = duration;
        
        console.log('Duración seleccionada:', duration);
        console.log('Valor del input:', document.getElementById('duracion').value);
    }
    
    // Select "Permanente" by default
    const defaultBadge = document.querySelector('.duration-badge[data-duration="0"]');
    if (defaultBadge) {
        selectDurationBadge(defaultBadge);
        console.log('Badge por defecto seleccionado');
    }
    
    // Form submission debug
    document.getElementById('nuevoUsuarioForm').addEventListener('submit', function(e) {
        const duracion = document.getElementById('duracion').value;
        console.log('Formulario enviado - Duración:', duracion);
        alert('Duración seleccionada: ' + duracion + ' meses');
    });
});
</script>
{% endblock %}
